#pragma once

namespace academic_life {

    enum class EnumScore
    {
        HEALTH,
        ESPB
    };

    enum LifestyleChange {
        Cigarette = -30,
        Beer = -20,
        WheyProtein = 50,
        fishMeal = 40,
        Apple = 20
    };  
}